// module.exports

const sum = (a,b) => a+b;
const sub= (a,b) => a-b;
const mul = (a,b) => a*b;

let abc=
{
    sum : sum , 
    sub : sub , 
    mul : mul 
};
module.exports = abc; //abc is object we made
